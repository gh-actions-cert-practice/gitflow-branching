name: 'Main CD workflow'

on:
  push:
    branches:
      - 'feature/**'
    paths-ignore:
      - '.github/**'

jobs:
  main-CD:
    runs-on: ubuntu-latest
    steps:
      - name: 'Deployment step'
        run: |
          echo "Deploying"..."
          sleep 15s
          echo "Deployed!"